
      <div class="container">
      <div class="row">

         <div class="col-sm-5">
            <h4>Member Name</h4>
            </div>
         <div class="col-sm-5">
            <h4>Email Address</h4>
            </div>
      </div>
      {{#each members}}

      <div class="row">
         <div class="col-sm-5">{{this.dataValues.name}}</div>
         <div class="col-sm-5">{{this.dataValues.email}}</div>
         <div class="col-sm-2">
            <button class="btn btn-gray mb-2">remove this member</button>
         </div>

      </div>
      </div>
      {{/each}}